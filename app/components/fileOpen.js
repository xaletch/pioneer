function openFileDialog(){let e=document.getElementById("resume");e.click()}function updateFileName(){let e=document.getElementById("resume"),t=document.querySelector(".file-placeholder");if(e.files.length>0){let l=e.files[0].name;t.textContent=l,t.style.color="#FFF"}else t.textContent="Прикрепите файл"}document.addEventListener("DOMContentLoaded",()=>{let e=document.querySelector(".input-wrapper");e.addEventListener("click",openFileDialog);let t=document.getElementById("resume");t.addEventListener("change",updateFileName)});